import { defineAppConfig } from "nuxt/app";

// app.config.ts
export default defineAppConfig({
  ui: {
    primary: 'primary',
    secondary: 'secondary',
    info: 'info',
    success: 'success',
    warning: 'warning',
    error: 'error' 
  }
})